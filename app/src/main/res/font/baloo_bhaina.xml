<?xml version="1.0" encoding="utf-8"?>
<font-family xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- regular -->
    <font android:font="@font/baloo_bhaina_regular"
        android:fontStyle="normal"
        android:fontWeight="400"></font>

    <!-- medium -->
    <font android:font="@font/baloo_bhaina_medium"
        android:fontStyle="normal"
        android:fontWeight="500"></font>

    <!-- italic -->
    <font android:font="@font/baloo_bhaina_regular"
        android:fontStyle="italic"
        android:fontWeight="400"></font>

    <!-- semi bold -->
    <font android:font="@font/baloo_bhaina_semi_bold"
        android:fontStyle="normal"
        android:fontWeight="600"></font>

    <!-- bold -->
    <font android:font="@font/baloo_bhaina_regular"
        android:fontStyle="normal"
        android:fontWeight="700"></font>
</font-family>