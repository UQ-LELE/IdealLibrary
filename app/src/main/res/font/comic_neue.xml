<?xml version="1.0" encoding="utf-8"?>
<font-family xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- regular -->
    <font android:font="@font/comic_neue_regular"
        android:fontStyle="normal"
        android:fontWeight="400"></font>

    <!-- italic -->
    <font android:font="@font/comic_neue_italic"
        android:fontStyle="italic"
        android:fontWeight="400"></font>

    <!-- light -->
    <font android:font="@font/comic_neue_light"
        android:fontStyle="normal"
        android:fontWeight="300"></font>

    <!-- light italic -->
    <font android:font="@font/comic_neue_light_italic"
        android:fontStyle="italic"
        android:fontWeight="300"></font>

    <!-- bold -->
    <font android:font="@font/comic_neue_bold"
        android:fontStyle="normal"
        android:fontWeight="700"></font>

    <!-- bold italic-->
    <font android:font="@font/comic_neue_bold_italic"
        android:fontStyle="italic"
        android:fontWeight="700"></font>
</font-family>